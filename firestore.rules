rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Blanket secure rule requiring authentication for reads/writes across all documents.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
